# Snapshot - 2026-08-30

## 🎯 O que foi feito hoje
- Criação do projeto do zero: cardápio digital "Forno & Brasa" (pizzaria/hamburgeria fictícia, peça de portfólio)
- Scaffold Next.js 16 + React 19 + TypeScript + Tailwind CSS v4
- Aplicação da paleta enviada pelo Marcelo (coolors)
- `src/lib/menu.ts`: 5 categorias e 27 itens com descrição, preço, selos e foto
- Componentes: Header, Hero, CategoryNav (scrollspy), MenuSection, MenuCard, ItemModal, CartDrawer, FoodImage, Tag, Footer
- `CartContext` com persistência em localStorage
- Checkout que monta o pedido formatado em mensagem do WhatsApp
- README, progress.md, .env.example
- Build de produção validado (passa limpo)
- Revisão visual no navegador (localhost:3000) categoria por categoria

## 📝 Decisões técnicas importantes
- Página única client-side (`page.tsx` "use client") — cardápio não precisa de SSR/DB nessa fase
- Cardápio como array em `lib/menu.ts` (sem backend) — YAGNI pra um portfólio
- Fotos via links diretos do Unsplash (sem API key, sem next/image) + `<FoodImage>` com fallback de marca
- Tema dark fixo (apetitoso, combina com a paleta vinho/laranja)

## ⚠️ Problemas encontrados e soluções
- Problema: `create-next-app` falhou (`EEXIST`/`EACCES` no `~/.npm/_cacache`)
  Solução: `npm install --cache "Desktop/Projetos AI/.npm-cache-vdt"`
- Problema: 3 fotos do Unsplash com assunto errado (Coca 2L=Pepsi, Limonada=bar, Nutella=yakisoba)
  Solução: Coca e Limonada trocadas por IDs conferidos no navegador; Nutella ficou com placeholder da marca
- Problema: Next 16 gera AGENTS.md/CLAUDE.md dentro do projeto
  Solução: `agentRules: false` no next.config.ts

## 🔧 Configurações adicionadas
- `next.config.ts`: `turbopack.root` + `agentRules: false`
- Pasta de cache npm dedicada: `Desktop/Projetos AI/.npm-cache-vdt`

## 📊 Estado atual do projeto
- MVP visual 100% funcional em localhost
- Build de produção OK
- Ainda não versionado em git nem deployado

## 🚧 Próxima sessão (To-do)
- Coletar feedback do Marcelo (nome, itens, cores, layout)
- Ajustar o que ele pedir
- Se aprovar: `git init` + commit + deploy Vercel via CLI
- (opcional) painel admin / backend de pedidos

## 💡 Observações importantes
- Servidor dev sobe com `npm run dev` na pasta do projeto
- Número de WhatsApp e endereço são fictícios (`src/lib/restaurant.ts`)
- O carrinho fica salvo no navegador — pra "zerar", limpar o localStorage ou usar o botão "Limpar pedido"
